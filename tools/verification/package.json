{
  "name": "@scratch-ai/verification",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cross-platform verification scripts and workflow fixtures for Scratch AI Coach",
  "scripts": {
    "build": "node -e \"console.log('verification workspace uses source scripts; no build step required')\"",
    "test": "node --test tests/**/*.test.mjs"
  },
  "dependencies": {
    "@scratch-ai/shared": "0.1.0",
    "yauzl": "^3.3.0"
  }
}
