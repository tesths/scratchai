请根据以下材料生成课堂排错包。

教学 brief:
{{briefJson}}

规划结果:
{{planJson}}

角色脚本包:
{{rolePacksJson}}

学生提示包:
{{hintsJson}}

输出 JSON 字段只能包含：
- title
- debugChecklist
- roleSpecificFixes
- whenToIntervene

其中：
1. debugChecklist 每个元素字段只能包含 symptom,askStudent,quickCheck,minimalFix,verify。
2. roleSpecificFixes 必须与角色一一对应；每个元素字段只能包含 name,watchFor,microFixes。
3. whenToIntervene 是老师应当接手演示的时机数组。
